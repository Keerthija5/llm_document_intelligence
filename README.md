# LLM-Based Document Intelligence System

A simple LLM-based system for processing unstructured documents, comparing model outputs, and generating structured insights.

---

## Overview

This project focuses on building a document intelligence pipeline using Large Language Models (LLMs) to process unstructured business text and convert it into structured insights.

The system is designed to handle real-world document types such as:

* support tickets
* maintenance reports
* meeting notes

and automatically extract useful information from them.

---

## What I implemented

* Built a pipeline to process multiple text documents
* Used Hugging Face models for summarization
* Compared two models: **BART** and **T5**
* Implemented a rule-based evaluation system to compare outputs
* Extracted action items and keywords using NLP techniques
* Generated structured outputs in JSON and CSV formats

---

## Workflow

```text
Input Text → Summarization (BART & T5) → NLP Processing → Evaluation → Output
```

---

## Model Comparison

Two models were used:

* **BART** → produces more fluent and structured summaries
* **T5** → sometimes generates longer outputs but formatting is less consistent

The comparison is based on:

* sentence quality
* formatting
* readability

---

## Results and Observations

* BART summaries were generally cleaner and easier to read
* T5 sometimes produced longer outputs but less structured
* In some cases both models performed similarly (tie)
* The evaluation step helped in deciding which output is better
* The pipeline successfully converted raw text into structured insights

---

## Example Output

### JSON Output (Model Comparison)

This output shows:

* summaries generated by both models
* evaluation scores
* selected preferred model

![Model Output](document_intelligence_output.png)

*This output shows how both models are evaluated side by side before selecting the preferred summary.*

---

### CSV Output (Structured Results)

This output shows:

* combined results for multiple documents
* extracted action items and keywords
* model comparison scores

![CSV Output](csv_output.png)

*This tabular output makes it easier to compare model behavior across documents and review structured results at scale.*

---

## Tech Stack

* Python
* Hugging Face Transformers
* Pandas
* Basic NLP (regex, text processing)

---

## Project Structure

```text
src/
  main.py
  llm_processor.py
  parser.py
  evaluator.py
  exporter.py

input/
requirements.txt
README.md
```

---

## Running the project

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 src/main.py
```

---

## Real-world relevance

This type of system can be useful in real-world scenarios like:

* processing support tickets
* analyzing maintenance reports
* extracting action items from meeting notes

It helps reduce manual effort and organize information in a structured way.

---

## Key takeaway

This project showed that model comparison is important even for a simple document pipeline. In my experiments, BART generally produced cleaner and more structured summaries, while T5 was useful for comparison but less consistent in formatting. Building an evaluation step made the pipeline more reliable and helped move beyond using a single model without validation.

---

## Author

Keerthija
M.Eng. Information Technology (Specialization: Artificial Intelligence)

